services:
  dev:
    image: yrobotics/y_boat_core:${DOCKER_IMAGE}
    container_name: boat_dev
    network_mode: host
    privileged: true
    stdin_open: true
    tty: true
    volumes:
      - ${WORKSPACE_SRC:-./src}:${WORKSPACE_DIR:-/workspace}/src
      - /dev:/dev
    environment:
      - ROS_DISTRO=${ROS_DISTRO:-jazzy}
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-10}